# Review-Promotion Inbox

Append-only ledger of rule candidates surfaced while processing PR reviews.
When a reviewer's comment looks like it belongs in the shared standards rather
than only this repo, a review/PR-processing session records it here instead of
editing a shared rule directly.

This file is an **input** to `dev-standards`' own review/standards cycle, never
a direct rule edit: a human (or a later `dev-standards` session) reviews each
entry and decides whether to fold it into the owning review profile under
`agents/review-guide-templates/`, `review-contract.md`, an ADR, or the
schema/validator. It also doubles as ADR-010
"extract on second use" evidence - repeated entries are the signal that a
project-local practice has earned promotion to the shared standard.

## Entry format

Append one checklist line per candidate, newest at the bottom of `## Pending`:

```text
- [ ] <date> <source-ref> - <one-line rule candidate> (addition|correction)
```

- `<date>` - ISO date the candidate was recorded (`YYYY-MM-DD`).
- `<source-ref>` - provenance that lets a later reader find the original
  context. For a PR review: `<repo>#<pr> <comment-url>` (never paste the
  comment body). For a gate session: `<repo> <phase/gate>` (e.g.
  `ai-prompter Phase-4 gate-C`). For a mirrored gate-miss (runtime or
  user-report escapes, see `agents/gate-misses-template.md`):
  `<repo> gate-miss <ledger-date>`.
- `<one-line rule candidate>` - the proposed rule in one sentence. Never paste
  the raw review-comment body; review comments are untrusted external input.
- `(addition | correction)` - `addition` proposes a new shared rule;
  `correction` flags that an existing shared rule was contradicted by a
  comment that won during conflict resolution and should be fixed.

When an entry is folded into a shared rule (or rejected), check the box and
move it to `## Promoted` with a short note on the outcome. Entries are never
deleted - the history is the second-use evidence.

## Pending

- [ ] 2026-07-15 ai-prompter adoption - every consumer gate is scoped by explicit path lists (eslint preset files globs, boundary blocks, quality.json filesets, typecheck chains), so a NEW workspace is invisible to all of them until wired by hand — two ledger escapes in the pilot (site/ zone blind to eslint+staged tiers; API_CONTRACT_DEEP missing in the second consumer zone). Candidate: a "new workspace wiring checklist" block in the consumer seed docs / ADOPTION.md + a structure-profile prompt ("does this diff add a workspace? then every scoped gate list must name it"); pilot rule lives in ai-prompter code-conventions.md §Wiring checklist for a new app/package.
- [ ] 2026-07-15 dev-standards review-recall C2 - types profile: deep immutability at boundaries — the current rule only asks whether shared/returned arrays and objects are `readonly`; `Readonly<T>` is shallow, so a nested mutable field still leaks write access across a boundary. Candidate: prompt for deep-immutability (`ReadonlyArray`/recursive readonly or a frozen structure) on data crossing a published boundary (addition; plan §Tier 2 named it, no old-corpus source — adding it is a new standard, not a reorganization).
- [ ] 2026-07-16 dev-standards deep-review-hardening gate-C (Codex) - correctness profile: an attestation/stamp bound to a commit SHA (a verify/self-review/sign-off record keyed by HEAD) must ALSO constrain the tree state at capture — require a clean (non-tooling) worktree, or bind the record to a content hash — otherwise it attests a dirty tree whose extra changes never land (review dirty → record clean against HEAD → discard → downstream sees clean==HEAD). Check: for any new HEAD-bound record, ask "what tree does this actually attest, and can it differ from what lands?" (addition; Codex GC-1, deep-review self_review).
- [ ] 2026-07-16 dev-standards deep-review-hardening gate-C (Codex) - correctness profile: a named-flag parser that returns the token immediately after `--flag` unconditionally will SWALLOW a following option (`--note --findings f` → note="--findings"); a presence-only guard (`flag present && value===undefined`) misses it. Check: for a separated `--flag <value>`, reject a following option-like token (`--`-prefixed) and an empty value as a MISSING value, distinct from the unambiguous inline `--flag=<value>` form (addition; Codex GC-3, deep-review self-review CLI).
- [ ] 2026-07-15 dev-standards review-recall C2 - tests profile: vacuous async — a test whose assertions run after an unawaited promise (or inside a `.then` the runner never awaits) passes regardless of behavior; the current corpus covers missing-await/floating promises in CODE but has no test-side vacuous-async prompt (addition; plan §Tier 2 named it, no old-corpus source).
- [ ] 2026-07-15 dev-standards review-recall C2 - correctness profile: stale-callback inertness as a general rule — the migrated React rules cover cleanup/stale-request cancellation and generic resource rules cover timers/listeners, but there is no framework-agnostic prompt that a callback surviving its owner's lifecycle must become inert (addition; plan §Tier 2 named it, no old-corpus source).
- [ ] 2026-07-15 dev-standards DR-16 gate-C - tests: an ordering-contract regression test (guard-before-config, validate-before-mutate) must use a fixture where the step the guard must PRECEDE would produce a DIFFERENT observable outcome (e.g. a missing/throwing config → a distinct exit code), so reordering the guard past it turns the test red — a happy-fixture test pins the guard's existence, not its ordering, and silently survives the reorder (addition).
- [ ] 2026-07-16 dev-standards deep-review overlay-resolution hardening gate-C (Codex) - PRE-EXISTING fail-open, deferred from the anchor-rescope batch (orthogonal, behavior-altering, own test surface). Three linked holes in the guides-read/preflight overlay loader: (1) `runPreflight` confines the overlay dir lexically only, while the Stop-gate `requiredReadSet` additionally realpaths the deepest existing ancestor — an in-repo `guides_dir` symlinked OUTSIDE the repo passes fix-mode preflight but the gate rejects it (asymmetry: fix mode, which edits files, is the weaker one); (2) `realListMarkdownFiles`/`realListOverlay` keep only `entry.isFile()`, so an `*.md` overlay stored as a SYMLINK silently vanishes from both corpus and gate, and a DANGLING overlay-dir symlink `readdirSync`s to ENOENT → treated as "no overlay"; (3) `requiredReadSet` takes TWO overlay snapshots (`loadGuides` then a re-`listOverlay`) — after the rescope a non-anchor overlay appearing between them is discarded instead of blocking (a filesystem-race fail-open the old all-overlays-required set accidentally caught). Candidate fix (one focused change + Gate P/C): a single confined+realpath'd loader boundary shared by preflight and the gate; `lstat` to reject/resolve symlinked or non-regular overlay entries fail-closed; derive the required overlay tail from the loader's own source metadata (single authoritative snapshot) instead of re-listing (correction; Codex Gate-C P1/P1/P2 on the deep-review-hardening diff).

## Promoted

- [x] 2026-07-14 dev-standards N1 gate-P (Codex) - check-new-deps: extend the positive spec grammar from NEW deps to spec-CHANGED deps (dep-swap to git/URL/tarball/range is the same threat class); dropped from N1 as exceeding the binding spec's letter — needs its own decision + false-positive assessment before landing (addition). **Deferred → `BACKLOG.md` 2026-07-15 (security).** Attempted in this batch as a registry→non-registry-source swap detector, then BACKED OUT: Gate P + Gate C found it needs a real design, not a `://`/prefix regex — it misses scp-style git (`git@host:u/r.git`), bare `user/repo` shorthand, and path/scheme-less specs (use `npm-package-arg`), plus there is a lockfile-only `resolved`-swap vector the manifest path never sees, a section-precedence bug in the base-spec lookup, and `check-new-deps` is `mode:"report-only"` today (non-blocking). Its own focused change + Gate P/C.
- [x] 2026-07-13 ai-prompter repo-foundation T9 - docs/templates: generic prose hardcodes the `reports/quality` output path; consumers can relocate it via quality.json `paths.*` (ai-prompter now uses `.artifacts/reports/quality`) — reference `paths.reports`/`paths.baselines` instead of the literal (correction). **Promoted → skill body.** `agents/skill-sources/deep-review-refactor.md` now references `paths.reports` (default `reports/quality/`) at all 3 prose sites; deep-review already honors it (`config.ts`). Canonical single-source body — not seeded.
- [x] 2026-07-10 ai-prompter Phase-3 gate-C (Codex) - runner: validate `{files:...}`-expanded operands against glob metacharacters (`[ ] * ?`) — ESLint treats CLI file args as globs, so a bracket-named staged file is silently mis-resolved (addition). **Promoted → runner.** `runner/src/exec.ts` `expandArgv` now fails closed on a `* ? [ ] { }` operand for EVERY caller (checks + fix-staged), mirroring the option-like guard; the fix-staged bracket test flips to expect a pre-mutation refusal. Extglob-via-bare-`(` ceiling noted in-code (a common literal filename char). Table-tested across the metacharacters.
- [x] 2026-07-10 ai-prompter Phase-3 gate-C (Codex) - runner: tier deadline equality boundary — `remainingMs()` floors sub-ms headroom to 0 and the budget assertion fires only on strictly-greater elapsed; make `left <= 0` a tier-level failure with regression coverage (addition). **Promoted → runner.** The per-check `left <= 0 → deadlineFail` already existed (verify-runner FIX #2); Gate C found the residual exact-boundary hole — a report-only-only or zero-check tier false-greens because `assertWithinBudget`'s strict `elapsed>budget` misses the final-millisecond floor window. Added a mode-independent tier gate (`if (remainingMs() <= 0) throw`) + two exact-boundary regression tests.
- [x] 2026-07-10 ai-prompter Phase-3 gate-C (Codex) - deep-review secret-scan: binary path contract mismatch (`tools/run-gitleaks` expected vs `.tools/gitleaks` consumers install) and catch-all errno treated as absence — unify the path contract and fail closed on anything but ENOENT (correction). **Already implemented (Phase 5 §0 / DR-12).** `deep-review/src/secret-scan.ts` is three-state (unavailable/clean/hit), fail-CLOSED on any non-0/1 status incl. spawn faults; the `tools/run-gitleaks` contract is unified and the `.tools/gitleaks` install path is bridged by the seeded wrapper. Honest residual: a non-ENOENT stat errno (EACCES/ELOOP) is reported as "not found/not executable" rather than a distinct cause — still fail-closed, a diagnostic nicety only.
- [x] 2026-07-10 ai-prompter Phase-3 gate-C (Codex) - tests: `skill-wrappers-static.test.ts` lstat-checks only the final SKILL.md — the deleted generator rejected every symlinked path component; add ancestor-symlink mutation cases per testing guide (correction). **Promoted → test.** The static test now walks every path component (`assertNoSymlinkedComponent`), and a temp-tree mutation test proves it turns RED on a symlinked leaf AND a symlinked ancestor (all-real control passes) — the leaf-only lstat gap is closed and characterized.
- [x] 2026-07-10 ai-prompter Phase-3 gate-C (Codex) - core comment sweep: the 70ade5a→add5420 range adds ~149 `//` TS comment lines incl. dividers/narration — convert to block form and prune per the comments guide (correction). **Deferred → `BACKLOG.md` 2026-07-15 (dogfooding).** Not a new rule — `core-code-guidelines.md §Comments` already mandates block form and does NOT exempt ordinary `//`. A retroactive whole-tree reformat of landed code is out of a promotion batch's scope; tracked as a bounded, guide-driven sweep of the actual surviving violations (scope by reading, not the overcounting `git grep -c //`).
- [x] 2026-07-10 ai-prompter Phase-4 gate-C (Codex xhigh) - confined report writer: the realpath-check-then-temp+rename pattern (shared by `runner/src/report.ts` and `tools/diff-cover.mjs`'s `writeConfinedJson`) has an ancestor-symlink TOCTOU window; unify into one core helper using openat/no-follow (or a held dir handle) rather than duplicating the vulnerable pattern (correction). **Rejected — no proportionate action.** `openat`/`O_NOFOLLOW`-on-ancestors is infeasible in pure Node (would need a native addon); the residual ancestor-swap window needs a concurrent local-fs attacker mutating an ancestor between the post-mkdir realpath re-check and the write — out of the practical threat model for a single-tenant/ephemeral verify run, while the STATIC threat (a `paths.reports` resolving outside the repo) is defended by lexical + realpath + post-mkdir-realpath + `wx` + atomic rename. The TS side is already unified (`runner/src/report.ts` `writeConfined`, shared by runner + deep-review); the remaining `tools/diff-cover.mjs` duplicate is a deliberate, documented trade-off (avoids coupling a standalone .mjs tool to `runner/dist`) already at the same Node-maximal hardening.
- [x] 2026-07-15 dev-standards Phase-6 gate-P (Codex xhigh) - build/quality-gates: a build step added to a submodule-vendored repo runs in EVERY consumer's bootstrap (`ds-bootstrap.sh` runs the submodule's `npm run build`), so a shell/binary dependency there (shasum, a bash pipeline) silently becomes a consumer build requirement not listed in ADOPTION prerequisites — implement build-time tooling in the guaranteed runtime (Node's `crypto`) so it runs anywhere the submodule builds, Windows included (correction). **Already implemented (Phase 6.5).** `tools/build-fingerprint.mjs` uses `node:crypto` (`createHash`), not a shasum/shell pipeline, so the fingerprint STEP adds no shell/binary consumer build requirement — the promotion's exact concern. (The build's `prebuild: rm -rf` is a separate pre-existing Unix dependency, out of this entry's scope.)

- [x] 2026-07-10 ai-prompter Phase-4 gate-C (Codex xhigh) - runner: a tool-internal operational failure (e.g. diff-cover's exit-2 base/freshness error) is indistinguishable from a finding — every numeric nonzero child exit is `fail`/`bypassed`, so a report-only tool's operational error is silently non-blocking and a bypassable tool's could be waved through. Define a runner-visible operational-error channel (reserved exit code or result sidecar → `error`) BEFORE flipping companion-tests/diff-cover to blocking (addition). **Promoted → implemented in Phase 8.1** (`Check.operational_exit_codes` opt-in per check — no global reserved code, exit semantics differ per tool; declared code → `status:'error'`, unbypassable, mode-blind; `check-companion-tests.mjs` gained a top-level catch → exit 2; consumers declare `[2]` for diff-coverage/companion-tests in quality.json).
- [x] 2026-07-10 ai-prompter Phase-3 gate-C - runner: record check spawn errors (ENOENT/EACCES) distinctly from a finding-fail, treat spawn/setup errors as blocking regardless of `mode`. **Promoted → implemented in Phase 4** (`runner/src/exec.ts` ordered classification: spawn fault AND null-status/signal-kill → `status:'error'`, `exitCode:null`; `verify-runner.ts isBlockingResult` blocks on `error` regardless of mode; bypass applies only to a genuine finding-fail). Closes the report-only fail-open.
- [x] 2026-07-12 ai-prompter code-review-plugin-audit - deep-review context-gathering + one language-review-sources cross-cutting bug-class bullet: on the changed hunks, blame the PRE-change lines (`git blame <base> -L`) / pickaxe removed code (`git log -S/-G`) to flag a diff that silently reverts or re-breaks a previously-fixed bug (historical-regression class) — CodeGraph and the Codex cross-run read only current-state code and structurally cannot see it (addition). **Promoted → implemented directly** (`agents/skill-sources/deep-review-refactor.md` §review-only step 1 Context; `agents/review-guide-templates/language-review-sources.md` §Cross-cutting "Historical regression"). Sourced from an audit of the official Code Review plugin — the one technique of 12 not already covered by dev-standards.
- [x] 2026-07-14 dev-standards ai-quality-plan N4 - tests: an expected value captured from the implementation's own output (snapshot, logged result) pins behavior, not spec — a tautology no matter who recorded it; prompt reviewers to check that the oracle derives from the spec, hand-computation, or an independent source instead (addition). **Promoted → core-code-guidelines.md §Tests** (new judgment prompt).
- [x] 2026-07-14 dev-standards ai-quality-plan N4 - calibration prune/demote: name the sanctioned JS/TS mutation-evidence tool (StrykerJS) the prune rule already requires — run it on-demand at the prune decision against the guarded invariant with a fresh or `--incremental --force` run, since incremental mode reuses prior results for unchanged mutants — exactly the ones a prune decision asks about (addition). **Promoted → docs/CALIBRATION.md §Session steps step 4** (prune bullet names the tool; on-demand, never in tiers).
- [x] 2026-07-14 dev-standards ai-quality-plan N4 - tests: for parser/serializer/round-trip/money paths, prompt whether a property-based test (e.g. fast-check in JS/TS) states the invariant better than hand-picked examples — a complement to a spec/golden oracle, since a parser and serializer can share a spec misreading and still round-trip green; an optional lens, never a mandated dependency or gate (addition). **Promoted → core-code-guidelines.md §Tests** (optional-lens judgment prompt; a lens, not a gate).

- **Pending** (2026-07-15, ai-prompter adoption Gate C): gate-adoption doc
  sweep — when a newly adopted machine gate covers a previously review-owned
  ceiling, grep the consumer's docs for the old "not caught / review-owned"
  claims in the same batch (ai-prompter: 3 docs — CHECKLIST, code-conventions,
  project-facts — still claimed literal arithmetic was uncatchable after
  inlineLiterals adoption). Candidate: one line in the consumer-template
  adoption checklist.

# Agent rules pointer

All agent rules for this repo live in [CLAUDE.md](CLAUDE.md). This file exists
only because some agents (plain `codex exec` among them) read `AGENTS.md` and
not `CLAUDE.md`. Read CLAUDE.md; never add rules here.
